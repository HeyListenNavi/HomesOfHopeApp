import { createIconSet } from "@expo/vector-icons";
import glyphMap from "@/assets/fonts/boxicons-brands.json"; 

const BrandBoxicon = createIconSet(glyphMap, "BrandBoxicons", "boxicons-brands.ttf");

export default BrandBoxicon;